# Full dev environment (core MFOpenFE deps + the benchmark harness's).
# For a lighter install, use pyproject.toml's extras instead:
#   pip install -e .                 # core only -- just enough to run MFOpenFE
#   pip install -e ".[benchmark]"    # + the comparison harness's deps

# Core data handling
pandas>=2.0
numpy>=1.24
tqdm>=4.60               # MFOpenFE progress reporting
pyarrow>=14.0            # parquet cache

# Dataset sources
# >=1.3 for sklearn.preprocessing.TargetEncoder; <1.6 because openfe 0.0.12
# calls mean_squared_error(..., squared=False), removed in sklearn 1.6.
scikit-learn>=1.3,<1.6   # sklearn built-ins + fetch_openml
openml>=0.14             # OpenML suites (CC18 / CTR23 / AMLB) + datasets
ucimlrepo>=0.0.7         # UCI datasets by name
kaggle>=1.6              # Kaggle competition downloads (needs ~/.kaggle/kaggle.json)

# Baseline model for the OpenFE parity check
lightgbm>=4.0

# AutoFE methods under benchmark
openfe>=0.0.12
featuretools>=1.31
autofeat>=2.1
matplotlib>=3.8          # openfe's FeatureSelector imports this at module load
setuptools<81            # featuretools/woodwork still import pkg_resources

# Tests
pytest>=7.4
